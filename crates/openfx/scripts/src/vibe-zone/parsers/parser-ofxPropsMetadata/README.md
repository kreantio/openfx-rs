This folder contains the parser for `ofxPropsMetadata.h`. How the parser results
should be formed is defined in `types.ts` and tested in `tests.ts`. The parser
itself is implemented by LLMs and located in `impl-by-llms/mod.ts`.
